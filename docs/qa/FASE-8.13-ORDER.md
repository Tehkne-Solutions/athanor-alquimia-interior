# Ordem das barreiras — Fase 8.13

```text
file.size
→ file.text()
→ text.length
→ JSON.parse
→ orçamento estrutural
→ checksum
→ versão
→ schema e política
→ conteúdo curado
→ sanitização
```

A primeira barreira que falha encerra o fluxo. Nenhuma etapa posterior é executada, nenhum arquivo é alterado e nenhuma recusa é persistida.

**Tehkné Solutions**
