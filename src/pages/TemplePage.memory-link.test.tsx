import { describe, expect, it } from 'vitest'

describe('Atrium practitioner memory entry point', () => {
  it('keeps the memory destination as a first-class temple route', () => {
    expect('/temple/memory').toBe('/temple/memory')
  })
})
